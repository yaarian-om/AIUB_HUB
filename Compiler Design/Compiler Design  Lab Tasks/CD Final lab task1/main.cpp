#include<bits/stdc++.h>
#include<stdlib.h>
using namespace std;
string s;
string l;
void iden (string cfg)
void real (string inf)
{
    ifstream input (inp);
    while (getline (input,i))
    {
        s=s+1+'\n';

    }
    iden(s);
    int n=s.lenght();
    vector<char> var;
    vector<char> var;

    int c;
    void display();
    void iden(string cfg)
    {
        for (int i=0;cfg[i]!='\0'i++)
        {
            if (cfg[i]=='-')
        }
    }
}

